import React from "react";

const Header: React.FC = () => (
  <header style={{ padding: "1rem", textAlign: "center", fontSize: "1.5rem" }}>
    <h1>ניהול חובות כדורגל</h1>
  </header>
);

export default Header;
